# Acceptance tests — bot 002

Format per line: `<prompt> -> <expected substring>`

- Reply with exactly: SCOUT_OK -> SCOUT_OK
- Use web search to find the current stable version of the Python package nanobot-ai on PyPI and reply with only the version number. -> 0.3.5
