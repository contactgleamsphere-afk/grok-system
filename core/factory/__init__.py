"""AI Factory core package.

Layers (see docs/ARCHITECTURE.md):
  registry  - machine-readable registries (models, tools, bots, components) with validation
  router    - model router: capability-aware selection + ordered fallback with health tracking
  botspec   - bot specification schema + validator (Bot Factory input contract)
"""
__version__ = "0.1.0"
