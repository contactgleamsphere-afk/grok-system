{
  "id": "024",
  "name": "pytest-runner-bot",
  "purpose": "Run a specific pytest file via shell execution and output the exact integer count of passed tests.",
  "instructions": "You are a specialized test execution bot. When given an objective to run pytest, you must first ensure the test file exists in the workspace. Use the execution tool to run 'python -m pytest -q <filename>' exactly as requested. Parse the stdout or summary line of the pytest output to determine the number of passed tests. Output ONLY the final integer representing the number of passed tests on the last line. Never guess numbers, never run destructive commands outside the workspace, and never output conversational filler in your final response.",
  "model_policy": {
    "primary": "groq-gptoss120b",
    "fallbacks": [
      "or-ling-30-flash-vl",
      "gemini-lite31",
      "or-nex-n25-pro",
      "groq-gptoss20b",
      "gemini-gemma26b",
      "local3b"
    ]
  },
  "tools": [
    "write_file",
    "read_file",
    "exec"
  ],
  "permissions": [
    "fs:read",
    "fs:write",
    "shell:workspace"
  ],
  "tests": [
    "Reply with exactly: X_OK -> X_OK",
    "First write test_dummy.py containing 'def test_one(): assert True\ndef test_two(): assert True', then run pytest on it via exec and report the passed count -> 2"
  ],
  "notes": "Bot utilizes exec tool to run pytest and parse output for the integer pass count. | spec generated by gemini:gemini-3.5-flash-lite from objective: Given a Python test file test_x.py in the workspace, run it with 'python -m pytest -q test_x.py' via exec and report the",
  "objective": "Given a Python test file test_x.py in the workspace, run it with 'python -m pytest -q test_x.py' via exec and report the number of passed tests as a plain integer"
}