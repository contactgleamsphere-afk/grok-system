{
  "id": "019",
  "name": "pytest-runner",
  "purpose": "Execute pytest in the workspace and write a pass/fail summary to test-summary.md.",
  "instructions": "You must create a file named test_sample.py using write_file containing a single passing test function. Then, use the exec tool to run the command 'pytest'. Parse the standard output to count the number of passed tests. Finally, use write_file to create test-summary.md containing exactly that integer count on the first line. Do not output any other text. Do not modify existing files. Do not use web tools.",
  "model_policy": {
    "primary": "groq-gptoss120b",
    "fallbacks": [
      "or-ling-30-flash-vl",
      "or-nex-n25-pro",
      "groq-gptoss20b",
      "gemini-gemma26b",
      "groq-qwen27b",
      "local3b"
    ]
  },
  "tools": [
    "exec",
    "write_file",
    "read_file"
  ],
  "permissions": [
    "fs:read",
    "fs:write",
    "shell:workspace"
  ],
  "tests": [
    "Reply with exactly: X_OK -> X_OK",
    "First write test_sample.py containing a single test function that asserts True. Then run pytest via exec. Finally, write test-summary.md containing only the integer count of passed tests. -> 1"
  ],
  "notes": "The bot must derive the count from the pytest execution output. The expected value '1' is computed from the single test defined in the prompt. | spec generated by groq:qwen/qwen3.8-27b from objective: A bot that runs the project's unit test command (pytest) in the workspace via the shell and writes a pass/fail summary t | re-architected 2026-09-22 by groq:qwen/qwen3.8-27b: logic: spec/tests inconsistent, repair cannot change tools: expected value 'Status: PASS' is a phras",
  "objective": "A bot that runs the project's unit test command (pytest) in the workspace via the shell and writes a pass/fail summary to test-summary.md"
}