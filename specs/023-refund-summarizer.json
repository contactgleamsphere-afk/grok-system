{
  "id": "023",
  "name": "refund-summarizer",
  "purpose": "Calculate total refunded amount from CSV and write summary",
  "instructions": "Read the file 'refunded.csv'. Parse the CSV content to identify the 'amount' column. Sum all numeric values in this column. Handle potential headers or non-numeric entries by skipping them. Write a single line to 'summary.txt' containing only the calculated total sum. Do not include any labels, currency symbols, or extra text in the output file. Ensure the file is written correctly before finishing. If the file does not exist, report an error.",
  "model_policy": {
    "primary": "groq-gptoss120b",
    "fallbacks": [
      "or-ling-30-flash-vl",
      "or-nex-n25-pro",
      "groq-gptoss20b",
      "gemini-gemma26b",
      "groq-qwen27b",
      "local3b"
    ]
  },
  "tools": [
    "read_file",
    "write_file"
  ],
  "permissions": [
    "fs:read",
    "fs:write"
  ],
  "tests": [
    "Reply with exactly: X_OK -> X_OK",
    "First write a file named refunded.csv containing the text 'id,amount\\n1,10.5\\n2,20.5\\n3,5.0' then read it, sum the amount column, and write the total to summary.txt. What is the total? -> 36.0",
    "First write a file named refunded.csv containing the text 'id,amount\\n1,100\\n2,200' then read it, sum the amount column, and write the total to summary.txt. What is the total? -> 300"
  ],
  "notes": "Uses only read and write tools. No shell execution required. | spec generated by groq:qwen/qwen3.8-27b from objective: Read refunded.csv, sum the values in the 'amount' column, and write a single line to summary.txt containing the total re",
  "objective": "Read refunded.csv, sum the values in the 'amount' column, and write a single line to summary.txt containing the total refunded amount."
}