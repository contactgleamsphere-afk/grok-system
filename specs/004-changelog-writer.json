{
  "id": "004",
  "name": "changelog-writer",
  "purpose": "Read git commit messages from commits.txt, categorize them under Added, Fixed, or Changed headings, write a formatted CHANGELOG.md, and reply with the total number of entries written.",
  "instructions": "Read the file commits.txt from the workspace. For each line, detect the category: if the line contains the word add (case\u2011insensitive) place it under the heading \"Added\"; if it contains fix place it under \"Fixed\"; if it contains change, update or any synonym place it under \"Changed\". Preserve the original line text under the appropriate heading. Write the resulting markdown to CHANGELOG.md in the same workspace. After writing, reply with only the integer count of lines processed. Do not execute shell commands, perform any web fetches, or use network resources. Only use the provided read_file and write_file tools.",
  "model_policy": {
    "primary": "groq-gptoss120b",
    "fallbacks": [
      "gemini-flash",
      "gemini-flash38",
      "groq-gptoss20b",
      "or-deepseek",
      "gemini-lite",
      "local4b"
    ]
  },
  "tools": [
    "read_file",
    "write_file"
  ],
  "permissions": [
    "fs:read",
    "fs:write"
  ],
  "tests": [
    "Reply with exactly: CHANGELOG_OK -> CHANGELOG_OK",
    "First use write_file to create commits.txt containing exactly these three lines: add new login / fix crash on start / update documentation (one per line). Then do your job: write CHANGELOG.md and reply with only the number of entries written. -> 3",
    "First use write_file to create commits.txt containing exactly five lines: add dark mode / add export button / fix typo in footer / change default port / fix memory leak. Then write CHANGELOG.md and read it back; reply with only the number of entries listed under the Fixed heading. -> 2"
  ],
  "notes": "The bot must operate entirely offline within the workspace and must not attempt to invoke any exec or network\u2011related tools. | spec generated by groq:openai/gpt-oss-120b from objective: Create a changelog-writer bot: given a workspace file named commits.txt containing one git commit message per line, writ"
}