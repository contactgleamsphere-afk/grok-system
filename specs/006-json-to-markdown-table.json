{
  "id": "006",
  "name": "json-to-markdown-table",
  "purpose": "Convert a JSON array in records.json to a Markdown table in table.md and report the number of data rows written.",
  "instructions": "Read records.json from the workspace, parse it as a JSON array of objects with identical keys, and generate a Markdown table. The first row must be a header with the object keys in their original order, followed by one row per object preserving order. Write the result to table.md. After writing, output only the exact count of data rows (i.e., the number of objects). Do not perform any network requests, shell commands, or modify files other than records.json and table.md. If records.json is missing or malformed, reply with the word ERROR and do not create table.md.",
  "model_policy": {
    "primary": "groq-gptoss120b",
    "fallbacks": [
      "gemini-flash",
      "gemini-flash38",
      "groq-gptoss20b",
      "or-deepseek",
      "gemini-lite",
      "local4b"
    ]
  },
  "tools": [
    "read_file",
    "write_file"
  ],
  "permissions": [
    "fs:read",
    "fs:write"
  ],
  "tests": [
    "Reply with exactly: X_OK -> X_OK",
    "First write records.json containing [{\"name\":\"Alice\",\"age\":30},{\"name\":\"Bob\",\"age\":25}] then run the conversion -> 2",
    "First write records.json containing [] then run the conversion -> 0"
  ],
  "notes": "The bot must only use the listed tools and respect the least\u2011privilege permissions. All file paths are relative to the workspace root. | spec generated by groq:openai/gpt-oss-120b from objective: Create a JSON-to-Markdown table bot: given a workspace file named records.json containing a JSON array of flat objects w"
}