{
  "id": "012",
  "name": "csv-column-sum",
  "purpose": "Sum the numeric values in the 'amount' column of a CSV file.",
  "instructions": "You are a data processing bot. When asked to sum a column, first ensure the input file exists in the workspace. If it does not, create it using write_file with the provided content. Then, read the file using read_file. Parse the CSV, identify the header row, and locate the column index for 'amount'. Iterate through the data rows, converting each value in that column to a number and adding it to a running total. Ignore non-numeric entries or empty cells. Finally, output the total sum as a single number on the last line of your response. Do not output any other text after the final number. Do not use external network tools. Do not modify the input file after creation.",
  "model_policy": {
    "primary": "groq-gptoss120b",
    "fallbacks": [
      "or-ling-30-flash-vl",
      "or-nex-n25-pro",
      "gemini-gemma26b",
      "groq-qwen27b",
      "groq-gptoss20b",
      "local3b"
    ]
  },
  "tools": [
    "read_file",
    "write_file"
  ],
  "permissions": [
    "fs:read",
    "fs:write"
  ],
  "tests": [
    "Reply with exactly: X_OK -> X_OK",
    "First write data.csv containing 'id,amount\\n1,10\\n2,20\\n3,30' then sum the amount column -> 60",
    "First write data.csv containing 'name,amount\\nA,5\\nB,15\\nC,2' then sum the amount column -> 22"
  ],
  "notes": "Added write_file tool and fs:write permission to allow fixture creation in tests. Tests are self-contained by instructing the bot to create the input file before processing. | spec generated by groq:qwen/qwen3.8-27b from objective: Create a CSV column-sum bot: given a workspace file data.csv with a header row, sum the numeric column named 'amount' an | re-architected 2026-09-22 by groq:qwen/qwen3.8-27b: test asks the bot to write a file but tools lack write_file: \"First write data.csv containing 'id,am",
  "objective": "Create a CSV column-sum bot: given a workspace file data.csv with a header row, sum the numeric column named 'amount' an"
}