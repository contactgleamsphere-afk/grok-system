{
  "id": "026",
  "name": "sqlite-query-bot",
  "purpose": "Answer questions about a SQLite database file in the workspace using read\u2011only queries via the mcp\u2011sqlite3 server.",
  "instructions": "You may only read files, query SQLite databases through the mcp:mcp-sqlite3 tool, and write minimal output files if explicitly asked. Never modify the database, never execute shell commands, and never perform network requests unless the prompt asks for a web fetch or search. Respond with only the requested answer; do not add explanations or extra text. If a request is unclear, ask for clarification.",
  "model_policy": {
    "primary": "groq-gptoss120b",
    "fallbacks": [
      "gemini-lite31",
      "or-nex-n25-pro",
      "or-ling-30-flash-sante",
      "or-ling-30-flash-fin",
      "groq-gptoss20b",
      "local3b"
    ]
  },
  "tools": [
    "read_file",
    "write_file",
    "mcp:mcp-sqlite3"
  ],
  "permissions": [
    "fs:read",
    "fs:write",
    "mcp:mcp-sqlite3"
  ],
  "tests": [
    "Reply with exactly: X_OK -> X_OK",
    "Using fixture.db, how many rows are in the users table? -> 3",
    "Using fixture.db, list the table names. -> users"
  ],
  "fixtures": [
    {
      "name": "fixture.db",
      "sql": "CREATE TABLE users(id INTEGER PRIMARY KEY, name TEXT);\nINSERT INTO users (name) VALUES ('Alice');\nINSERT INTO users (name) VALUES ('Bob');\nINSERT INTO users (name) VALUES ('Carol');"
    }
  ],
  "notes": " | spec generated by groq:openai/gpt-oss-120b from objective: Answer questions about a SQLite database file in the workspace using the mcp:mcp-sqlite3 server with read-only queries:  | re-architected 2026-09-24 by groq:openai/gpt-oss-120b: Every test prompt that uses the fixture database must name the file fixture.db explicitly (e.g. 'Usi",
  "objective": "Answer questions about a SQLite database file in the workspace using the mcp:mcp-sqlite3 server with read-only queries: list its tables, and count the rows in a named table. Declare a small fixture database for the tests."
}