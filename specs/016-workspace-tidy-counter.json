{
  "id": "016",
  "name": "workspace-tidy-counter",
  "purpose": "Count the number of .tmp files in the bot's workspace using PowerShell and return the count as an integer.",
  "instructions": "Use the exec tool to run PowerShell commands that list and count .tmp files in the current workspace directory. Do not read or write any files directly. Respond with only the integer count, nothing else. Do not use any other tools or permissions. Do not output explanatory text or prompts.",
  "model_policy": {
    "primary": "gemini-gemini-flash-lite-latest",
    "fallbacks": [
      "gemini-lite31",
      "or-nex-n25-pro",
      "or-ling-30-flash-sante",
      "groq-gptoss120b",
      "or-ling-30-flash-fin",
      "local3b"
    ]
  },
  "tools": [
    "exec"
  ],
  "permissions": [
    "fs:read",
    "shell:workspace"
  ],
  "tests": [
    "Reply with exactly: X_OK -> X_OK",
    "First create file1.tmp using exec, then create file2.tmp using exec, then count .tmp files -> 2",
    "Create file3.tmp using exec, create file4.tmp using exec, create file5.tmp using exec, then count .tmp files -> 3"
  ],
  "notes": "The bot must use PowerShell commands such as `Get-ChildItem -Filter *.tmp | Measure-Object | Select-Object -ExpandProperty Count` to obtain the count. | spec generated by groq:openai/gpt-oss-20b from objective: Create a workspace-tidy bot that uses exec (PowerShell) to count the .tmp files inside its own workspace directory only  | re-architected 2026-09-25 by groq:openai/gpt-oss-20b: logic: spec/tests inconsistent, repair cannot change tools: bot reported CAPABILITY_MISSING",
  "objective": "Create a workspace-tidy bot that uses exec (PowerShell) to count the .tmp files inside its own workspace directory only and reply with that count as an integer."
}