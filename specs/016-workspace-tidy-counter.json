{
  "id": "016",
  "name": "workspace-tidy-counter",
  "purpose": "Count .tmp files in the workspace directory using PowerShell and return the integer count.",
  "instructions": "You are a workspace tidy bot. Your sole task is to count the number of files ending in .tmp within the current workspace directory. Use the exec tool to run a PowerShell command that lists these files and counts them. Do not modify, delete, or create any files. Do not access directories outside the workspace. If no .tmp files exist, return 0. Always output only the final integer count as your last line. Never include explanations, markdown, or extra text in the final output.",
  "model_policy": {
    "primary": "groq-gptoss120b",
    "fallbacks": [
      "or-ling-30-flash-vl",
      "or-nex-n25-pro",
      "gemini-gemma26b",
      "groq-qwen27b",
      "groq-gptoss20b",
      "local3b"
    ]
  },
  "tools": [
    "exec"
  ],
  "permissions": [
    "shell:workspace"
  ],
  "tests": [
    "Reply with exactly: X_OK -> X_OK",
    "First write a file named test1.tmp using write_file. Then write a file named test2.tmp using write_file. Then use exec to count the .tmp files in the workspace and reply with the count. -> 2",
    "Use exec to count the .tmp files in the workspace and reply with the count. -> 0"
  ],
  "notes": "The bot relies on exec for PowerShell execution. The workspace is empty at test start, so tests that expect a non-zero count must first create the files using write_file. The final output must be strictly the integer count. | spec generated by groq:qwen/qwen3.8-27b from objective: Create a workspace-tidy bot that uses exec (PowerShell) to count the .tmp files inside its own workspace directory only ",
  "objective": "Create a workspace-tidy bot that uses exec (PowerShell) to count the .tmp files inside its own workspace directory only and reply with that count as an integer."
}