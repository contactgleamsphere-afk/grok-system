{
  "id": "012",
  "name": "csv-column-sum",
  "purpose": "Sum the numeric values in the 'amount' column of a CSV file and return the integer total.",
  "instructions": "Read the file 'data.csv' from the workspace. Parse the header row to identify the index of the column named 'amount'. Iterate through the remaining rows, extracting the value at that index. Convert each value to an integer, ignoring any non-numeric entries or empty cells. Sum all valid integers. Reply with the final sum as a single integer. Do not output any other text, explanations, or formatting. If the file is missing or the column is not found, reply with 'ERROR'.",
  "model_policy": {
    "primary": "groq-gptoss120b",
    "fallbacks": [
      "or-ling-30-flash-vl",
      "or-nex-n25-pro",
      "gemini-gemma26b",
      "groq-qwen27b",
      "groq-gptoss20b",
      "local3b"
    ]
  },
  "tools": [
    "read_file"
  ],
  "permissions": [
    "fs:read"
  ],
  "tests": [
    "Reply with exactly: X_OK -> X_OK",
    "First write data.csv containing 'id,amount\\n1,10\\n2,20\\n3,30' then read data.csv, sum the amount column, and reply with the total as an integer. -> 60",
    "First write data.csv containing 'name,amount\\nA,5\\nB,15\\nC,20' then read data.csv, sum the amount column, and reply with the total as an integer. -> 40"
  ],
  "notes": "Least privilege: only read_file is required. The bot must handle header parsing and numeric conversion robustly. | spec generated by groq:qwen/qwen3.8-27b from objective: Create a CSV column-sum bot: given a workspace file data.csv with a header row, sum the numeric column named 'amount' an"
}