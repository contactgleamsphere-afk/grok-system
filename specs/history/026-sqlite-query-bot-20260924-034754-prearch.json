{
  "id": "026",
  "name": "sqlite-query-bot",
  "purpose": "Answer questions about a SQLite database file in the workspace using read\u2011only queries via the mcp\u2011sqlite3 server.",
  "instructions": "You may read any file in the workspace and use the mcp:mcp-sqlite3 tool to run read\u2011only SQL queries. Never modify the database, never write arbitrary files unless explicitly asked, and never execute shell commands. Respond with only the final answer token on the last line. If a request is outside read\u2011only querying, reply with \"NOT_ALLOWED\".",
  "model_policy": {
    "primary": "groq-gptoss120b",
    "fallbacks": [
      "gemini-lite31",
      "or-nex-n25-pro",
      "or-ling-30-flash-sante",
      "or-ling-30-flash-fin",
      "groq-gptoss20b",
      "local3b"
    ]
  },
  "tools": [
    "read_file",
    "write_file",
    "mcp:mcp-sqlite3"
  ],
  "permissions": [
    "fs:read",
    "fs:write",
    "mcp:mcp-sqlite3"
  ],
  "tests": [
    "Reply with exactly: X_OK -> X_OK",
    "List the tables in the database fixture.db -> users",
    "How many rows are in the users table? -> 3"
  ],
  "fixtures": [
    {
      "name": "fixture.db",
      "sql": "CREATE TABLE users(id INTEGER PRIMARY KEY, name TEXT);\nINSERT INTO users(name) VALUES ('Alice');\nINSERT INTO users(name) VALUES ('Bob');\nINSERT INTO users(name) VALUES ('Carol');"
    }
  ],
  "notes": " | spec generated by groq:openai/gpt-oss-120b from objective: Answer questions about a SQLite database file in the workspace using the mcp:mcp-sqlite3 server with read-only queries: ",
  "objective": "Answer questions about a SQLite database file in the workspace using the mcp:mcp-sqlite3 server with read-only queries: list its tables, and count the rows in a named table. Declare a small fixture database for the tests."
}