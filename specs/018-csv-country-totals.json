{
  "id": "018",
  "name": "csv-country-totals",
  "purpose": "Aggregate order amounts by country from orders.csv into totals.csv",
  "instructions": "Read the file 'orders.csv' using read_file. Parse the CSV content, assuming the header contains 'country' and 'amount' columns. Sum the 'amount' values for each unique 'country'. Generate a new CSV string with the header 'country,total' followed by one row per country containing the country name and its calculated total. Write this content to 'totals.csv' using write_file. Do not use exec or shell commands. Do not modify the original orders.csv. Ensure numeric precision is maintained. If the file is missing or malformed, report an error clearly.",
  "model_policy": {
    "primary": "groq-gptoss120b",
    "fallbacks": [
      "or-ling-30-flash-vl",
      "or-nex-n25-pro",
      "gemini-gemma26b",
      "groq-qwen27b",
      "groq-gptoss20b",
      "local3b"
    ]
  },
  "tools": [
    "read_file",
    "write_file"
  ],
  "permissions": [
    "fs:read",
    "fs:write"
  ],
  "tests": [
    "Reply with exactly: X_OK -> X_OK",
    "First write orders.csv containing 'country,amount\\nUS,10\\nUS,20\\nUK,5' then read it, calculate totals per country, and write totals.csv. Reply with the total for US. -> 30",
    "First write orders.csv containing 'country,amount\\nFR,100\\nDE,200' then read it, calculate totals per country, and write totals.csv. Reply with the total for DE. -> 200"
  ],
  "notes": "Uses only fs:read and fs:write. No shell access required. Handles simple CSV aggregation. | spec generated by groq:qwen/qwen3.8-27b from objective: reads orders.csv and writes totals.csv with one row per country and the total amount for that country (header: country,t",
  "objective": "reads orders.csv and writes totals.csv with one row per country and the total amount for that country (header: country,total)"
}