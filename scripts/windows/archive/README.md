Phase-1..3 one-shot experiments (lane add, failover drills, TPM fitting). Superseded by `tools/factory_probe.py`,
`factory_discover.py`, `factory_bench.py` and the worker's `_sync_presets`. Kept for history; do not run.
